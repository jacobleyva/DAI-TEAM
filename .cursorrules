# DAI System — Cursor Wiring

Cursor reads this file at every session start. It's the Cursor-side equivalent of Codex's `developer_instructions` block, Claude Code's `CLAUDE.md`, and Gemini CLI's `GEMINI.md` — the entry point that loads DAI's doctrine, current focus, and skills catalog into the session before any work begins.

## Session Bootstrap

The synthesized startup context lives in `memory/session-context.md`. That single file catenates the constitution, the six-phase Algorithm, the ISC granularity discipline, the verification doctrine, effort tiering, identity, current focus, and the skills catalog.

Read `memory/session-context.md` at the start of every session. Treat it as the operating context for any non-trivial work. Regenerate it after any change to its sources with `tools/scripts/build_session_context.sh`.

## What To Follow

For any non-trivial work (multi-file, ambiguous, touching `core/`, `memory/`, or `projects/{name}/`):

- Run the six-phase Algorithm. See `core/ALGORITHM.md`. Phases: OBSERVE → THINK → PLAN → EXECUTE → VERIFY → LEARN. The single sanctioned back-edge is an ISC failure routing back to THINK (max 3 attempts before escalation).
- Create a WORK note at `memory/work/{slug}.md` (or `projects/{name}/WORK.md` for project work). The WORK note is the system of record.
- Write acceptance criteria per `core/ISC.md`. Every criterion is one binary tool probe. Apply the Splitting Test. Tag independent ISCs with `parallel_group:` so VERIFY can batch them in a single response — Cursor's underlying model (Claude / GPT / Gemini, depending on selection) will fire parallel tool calls when available.
- Hold to the verification doctrine. See `core/verification-doctrine.md`. Tool-verified evidence required before marking any ISC `[x]`.
- Read the constitution. See `core/constitution.md`. The NEVER / ALWAYS / BEFORE rules are non-negotiable defaults.

For skills, check `skills/skills-map.md` and read the matching `SKILL.md` before invoking the workflow.

For security work (review, threat-modeling, incident-response), route through `core/SECURITY_DOMAIN_OVERVIEW.md`.

## Cursor-Specific Notes

- **File format compatibility.** This file is `.cursorrules` at the repo root — the legacy single-file format that all Cursor versions support. Cursor v0.43+ also supports `.cursor/rules/*.mdc` (newer structured-rules format); the legacy `.cursorrules` continues to work. If your team adopts `.cursor/rules/`, the content of this file can be copied verbatim into `.cursor/rules/dai.mdc` with frontmatter `description: DAI workspace operating rules`, `globs: ["**/*"]`, `alwaysApply: true`.
- **Model selection.** Cursor lets the user pick the underlying model (Claude, GPT, Gemini, or others). DAI's `reasoning_effort:` field in the WORK note frontmatter maps to Cursor's model-tier selection: `low` → fast model, `medium` → mid-tier, `high` → top-tier. See `core/effort-tiering.md` for the vendor-mapping table.
- **Parallel tool calls.** Cursor inherits parallel-tool-call support from the selected underlying model. When backed by GPT-5 / Claude 4.x / Gemini 3 Pro, ISCs tagged `parallel_group:` will batch in one response. With smaller / older models, they fall back to sequential — the doctrine remains correct, just slower.
- **Per-skill agent specs.** Cursor reads vendor-specific prompts from skill agent files when you configure it to. For Cursor projects backed by Claude, drop a `skills/<skill-name>/agents/anthropic.yaml`; for GPT-backed, `openai.yaml`; for Gemini-backed, `google.yaml`. See `core/agent-composition.md` for the schema.

## Keep Higher-Priority Instructions in Force

This file adds DAI scaffolding on top of Cursor's defaults. It does not replace Cursor behavior or override the user's active prompt or per-file `@`-mention context. When in doubt, the active prompt wins; DAI doctrine wins on anything the active prompt doesn't speak to.

## Verifying the Wiring

In a fresh Cursor session in this repo, ask:

> What's in memory/session-context.md? Summarize what this workspace is and what rules I'm operating under.

If Cursor describes the constitution, the Algorithm, the ISC doctrine, the verification doctrine, the effort tiering, the identity, and the skills catalog — the wiring works. If Cursor says it doesn't see the file, check that the file exists (regenerate via `tools/scripts/build_session_context.sh`) and that Cursor's "Codebase Indexing" has finished for the repo.

## Related

- `~/.codex/config.toml` `developer_instructions` block — the Codex-side equivalent. See `core/CODEX_INTEGRATION.md`.
- `CLAUDE.md` — the Claude Code-side equivalent at repo root.
- `GEMINI.md` — the Gemini CLI-side equivalent at repo root.
- `core/AI_CLI_ADAPTERS.md` — per-vendor wiring conventions and adapter recipes for Aider, Copilot Chat, Continue, Cody, and other coding-assistant CLIs.
- `core/SECURITY_DOMAIN_OVERVIEW.md` — task routing across security skills.
- `core/REPO_INTEGRATION.md` — GitHub vs Azure DevOps wiring for the CI surface.
